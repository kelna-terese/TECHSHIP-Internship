let totalClasses = 63;
let attendedClasses = 57;

let attendancePercentage =
    (attendedClasses / totalClasses) * 100;

console.log(
    "\nAttendance Percentage:",
    attendancePercentage + "%"
);